const assert = require('assert');
const praticeModule = require('../index.js');

describe('praticeModule', () => {
  it('has a test', () => {
    assert(false, 'praticeModule should have a test');
  });
});
